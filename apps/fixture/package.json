{
  "name": "@kept/fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Kepler Coffee — the fixture application whose README claims KEPT verifies.",
  "scripts": {
    "dev": "next dev -p 3100",
    "build": "next build",
    "start": "next start -p 3100",
    "typecheck": "tsc -p tsconfig.json"
  }
}
